# Database — SQLite for local development, no setup required
DATABASE_URL="file:./dev.db"

# JWT — insecure default for local testing. Regenerate for any non-local use.
JWT_SECRET="karat-dev-secret-change-in-production"

# OpenRouter — required for the AI assistant feature.
# Get a key at https://openrouter.ai/keys
# If you don't have one, the rest of the app works fine without it —
# or visit the deployed app to see the AI features in action.
OPENROUTER_API_KEY=

# Google Places — required for address autocomplete in onboarding.
# Get a key at https://console.cloud.google.com (enable Places API).
# The form still works without it — you just type addresses manually.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
